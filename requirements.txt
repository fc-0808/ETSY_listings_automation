# GPT-5 / reasoning model support requires openai >= 1.70.
# Modern wheels include prebuilt jiter for Windows, fixing the older DLL issue.
openai>=1.70.0
httpx>=0.27.0
cloudinary>=1.40.0
openpyxl>=3.1.0
